# Offline Podcast Generator
Scripts to edit podcast feeds to point to different sources, useful to make a local cache with full descriptions.

## Usage
Install dependencies with `pip install -r requirements.txt`
To edit a feed run `main.py {feed_location} {podcast_directory} {server_name}`

To rename files to match episode-url tags run `rename.py {feed_location} {podcast_directory}`